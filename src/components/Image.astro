---
export interface Props {
  image: object;
  cssClass?: string;
}
const { image, cssClass = null } = Astro.props as Props;

---
<img src={image?.src} alt={image?.altText} height={image?.height} width={image?.width} class={cssClass}>
