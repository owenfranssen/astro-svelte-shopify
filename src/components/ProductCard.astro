---
const { product } = Astro.props

console.log(product)
---

<a href="{`/products/${product.handle}`}" class="group">
  <div class="w-full aspect-w-1 aspect-h-1 rounded-lg overflow-hidden sm:aspect-w-2 sm:aspect-h-3">
    <img {product.images.edges[0]?.node.src || 'blank.png'} alt={product.handle} class="w-full h-full object-center object-cover group-hover:opacity-75">
  </div>
  <div class="mt-4 flex items-center justify-between text-base font-medium text-gray-900">
    <h3>{ product.title }</h3>
    <p>$149</p>
  </div>
  <p class="mt-1 text-sm italic text-gray-500">Black</p>
</a>
